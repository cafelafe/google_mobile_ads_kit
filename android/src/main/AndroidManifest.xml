<!-- No `package` attribute: AGP 8+ rejects it ("Incorrect package found in
     source AndroidManifest.xml"). The namespace lives in build.gradle. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>

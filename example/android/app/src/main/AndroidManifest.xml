<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="google_mobile_ads_kit_example"
        android:name=".Application"
        android:icon="@mipmap/ic_launcher">
        <!-- Required by google_mobile_ads_kit. The Next-Gen SDK takes the App ID
             from code, but the plugin reads it from here so that initialize()
             needs no argument, exactly as in google_mobile_ads. Without this
             entry initialize() reports an error and no ad ever loads.

             This is Google's sample App ID — replace it with your own before
             shipping, or your ad units will not fill. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         The engine's text-selection features use this to offer
         "process text" actions. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
